# Install with: dbt deps --profiles-dir .
# dbt_utils supplies unique_combination_of_columns, used to assert the grain of
# mart_stock_metrics in models/marts/schema.yml.
packages:
  - package: dbt-labs/dbt_utils
    version: 1.1.1
