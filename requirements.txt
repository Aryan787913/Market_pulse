# Python dependencies for the MarketPulse data pipeline.
# Versions are pinned so the project builds identically on any machine.
# Install:  pip install -r requirements.txt

# Data extraction
# yfinance must track Yahoo's undocumented endpoints, which change without
# notice. 0.2.40 stopped returning any data ("possibly delisted; No timezone
# found" for every symbol) because it still spoke to a retired endpoint; 1.6.0
# is the version verified against the live API. It reaches Yahoo through
# curl_cffi rather than requests, which is installed as its dependency.
yfinance==1.6.0

pandas==2.2.2
numpy==1.26.4

# Database
psycopg2-binary==2.9.9
SQLAlchemy==2.0.30

# Configuration
python-dotenv==1.0.1

# Orchestration
apache-airflow==2.9.2

# Transformation
dbt-core==1.8.2
dbt-postgres==1.8.2

# Testing
pytest==8.2.2
